-- Create database
CREATE DATABASE IF NOT EXISTS ecoplayer;
USE ecoplayer;

-- Drop tables if they exist (in correct order to handle foreign keys)
DROP TABLE IF EXISTS delivery;
DROP TABLE IF EXISTS payment;
DROP TABLE IF EXISTS orders;
DROP TABLE IF EXISTS product;
DROP TABLE IF EXISTS recycle_item;
DROP TABLE IF EXISTS customer;

-- Create Customer table
CREATE TABLE customer (
    cus_id INT PRIMARY KEY AUTO_INCREMENT,
    cus_name VARCHAR(100) NOT NULL,
    cus_contact VARCHAR(15) NOT NULL,
    cus_date DATE NOT NULL
);

-- Create Product table
CREATE TABLE product (
    pro_id VARCHAR(20) PRIMARY KEY,
    pro_name VARCHAR(100) NOT NULL,
    pro_materials VARCHAR(255) NOT NULL
);

-- Create RecycleItem table
CREATE TABLE recycle_item (
    item_id INT PRIMARY KEY AUTO_INCREMENT,
    item_materials VARCHAR(100) NOT NULL,
    item_kg DOUBLE NOT NULL,
    item_price DOUBLE NOT NULL,
    date DATE NOT NULL
);

-- Create Orders table
CREATE TABLE orders (
    order_id INT PRIMARY KEY AUTO_INCREMENT,
    pro_name VARCHAR(100) NOT NULL,
    bile_id VARCHAR(50) NOT NULL,
    cus_id INT NOT NULL,
    FOREIGN KEY (cus_id) REFERENCES customer(cus_id) ON DELETE CASCADE ON UPDATE CASCADE
);

-- Create Payment table
CREATE TABLE payment (
    pay_id INT PRIMARY KEY AUTO_INCREMENT,
    order_id INT NOT NULL,
    pay_value DOUBLE NOT NULL,
    FOREIGN KEY (order_id) REFERENCES orders(order_id) ON DELETE CASCADE ON UPDATE CASCADE
);

-- Create Delivery table
CREATE TABLE delivery (
    delivery_id INT PRIMARY KEY AUTO_INCREMENT,
    order_id INT NOT NULL,
    delivery_location VARCHAR(255) NOT NULL,
    FOREIGN KEY (order_id) REFERENCES orders(order_id) ON DELETE CASCADE ON UPDATE CASCADE
);

-- Insert sample data (optional)

-- Sample Customers
INSERT INTO customer (cus_name, cus_contact, cus_date) VALUES
('John Doe', '0771234567', '2024-01-15'),
('Jane Smith', '0772345678', '2024-01-20'),
('Bob Wilson', '0773456789', '2024-02-01');

-- Sample Products
INSERT INTO product (pro_id, pro_name, pro_materials) VALUES
('P001', 'Recycled Plastic Bottle', 'PET Plastic'),
('P002', 'Eco-friendly Bag', 'Recycled Paper'),
('P003', 'Glass Container', 'Recycled Glass');

-- Sample Recycle Items
INSERT INTO recycle_item (item_materials, item_kg, item_price, date) VALUES
('Plastic Bottles', 5.5, 550.00, '2024-01-10'),
('Paper', 10.0, 800.00, '2024-01-15'),
('Glass', 7.5, 600.00, '2024-01-20'),
('Metal Cans', 3.2, 400.00, '2024-02-01');

-- Sample Orders
INSERT INTO orders (pro_name, bile_id, cus_id) VALUES
('Recycled Plastic Bottle', 'BILL001', 1),
('Eco-friendly Bag', 'BILL002', 2),
('Glass Container', 'BILL003', 3);

-- Sample Payments
INSERT INTO payment (order_id, pay_value) VALUES
(1, 1500.00),
(2, 800.00),
(3, 1200.00);

-- Sample Deliveries
INSERT INTO delivery (order_id, delivery_location) VALUES
(1, '123 Main St, Colombo'),
(2, '456 Park Ave, Kandy'),
(3, '789 Beach Rd, Galle');

-- Display table structures
SHOW TABLES;

