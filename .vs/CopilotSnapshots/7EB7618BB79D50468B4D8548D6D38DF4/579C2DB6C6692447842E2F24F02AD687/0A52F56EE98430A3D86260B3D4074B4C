package lk.ijse.recycle.controller;

import javafx.event.ActionEvent;
import javafx.fxml.FXML;
import javafx.fxml.Initializable;
import javafx.scene.control.Alert;
import javafx.scene.control.ButtonType;
import javafx.scene.layout.AnchorPane;
import lk.ijse.recycle.util.Navigation;

import java.net.URL;
import java.util.ResourceBundle;

public class DashbordController implements Initializable {

    @FXML
    private AnchorPane mainContent;

    @Override
    public void initialize(URL url, ResourceBundle resourceBundle) {
        try {
            Navigation.navigateToPane(mainContent, "welcome");
        } catch (Exception e) {
            e.printStackTrace();
        }
    }

    @FXML
    void btnCustomer(ActionEvent event) {
        try {
            Navigation.navigateToPane(mainContent, "customer");
        } catch (Exception e) {
            e.printStackTrace();
        }
    }

    @FXML
    void btnOrder(ActionEvent event) {
        try {
            Navigation.navigateToPane(mainContent, "order");
        } catch (Exception e) {
            e.printStackTrace();
        }
    }

    @FXML
    void btnProduct(ActionEvent event) {
        try {
            Navigation.navigateToPane(mainContent, "product");
        } catch (Exception e) {
            new Alert(Alert.AlertType.ERROR, "FXML Loading Error:" +e.getMessage()).show();
            e.printStackTrace();

        }
    }

    @FXML
    void btnPayement(ActionEvent event) {
        try {
            Navigation.navigateToPane(mainContent, "payment");
        } catch (Exception e) {
            e.printStackTrace();
        }
    }

    @FXML
    void btnDelivery(ActionEvent event) {
        try {
            Navigation.navigateToPane(mainContent, "Delivery");
        } catch (Exception e) {
            e.printStackTrace();
        }
    }

    @FXML
    void btnRecycle(ActionEvent event) {
        try {
            Navigation.navigateToPane(mainContent, "recycleitem");
        } catch (Exception e) {
            e.printStackTrace();
        }
    }
}























/*
package lk.ijse.recycle.controller;

import javafx.event.ActionEvent;
import javafx.fxml.FXML;
import javafx.fxml.FXMLLoader;
import javafx.scene.Parent;
import javafx.scene.control.Button;
import javafx.scene.layout.AnchorPane;

import java.io.IOException;

public class DashbordController {

    @FXML
    private AnchorPane root;

    @FXML
    private Button buttonCustomer;
    @FXML
    private Button buttonProduct;
    @FXML
    private Button buttonOrder;
    @FXML
    private Button buttonPayment;
    @FXML
    private Button buttonDelivery;
    @FXML
    private Button buttonRecycleItems;

    // Auto called by JavaFX
    @FXML
    public void initialize() {
        try {
            loadView("customer.fxml");
            setActiveButton(buttonCustomer);
        } catch (IOException e) {
            e.printStackTrace();
        }
    }

    // All buttons use this method
    @FXML
    private void clickButton(ActionEvent event) {
        Object source = event.getSource();

        if (source == buttonCustomer) {
            safeLoad("customer.fxml", buttonCustomer);
        } else if (source == buttonProduct) {
            safeLoad("product.fxml", buttonProduct);
        } else if (source == buttonOrder) {
            safeLoad("order.fxml", buttonOrder);
        } else if (source == buttonPayment) {
            safeLoad("payement.fxml", buttonPayment);
        } else if (source == buttonDelivery) {
            safeLoad("delivery.fxml", buttonDelivery);
        } else if (source == buttonRecycleItems) {
            safeLoad("recycleItems.fxml", buttonRecycleItems);
        }
    }

    private void safeLoad(String fxml, Button button) {
        try {
            loadView(fxml);
            setActiveButton(button);
        } catch (IOException e) {
            e.printStackTrace();
        }
    }

    private void loadView(String fxml) throws IOException {
        Parent view = FXMLLoader.load(
                getClass().getResource("/lk/ijse/recycle/view/" + fxml)
        );
        root.getChildren().setAll(view);
    }

    private void setActiveButton(Button active) {
        buttonCustomer.getStyleClass().remove("active-menu");
        buttonProduct.getStyleClass().remove("active-menu");
        buttonOrder.getStyleClass().remove("active-menu");
        buttonPayment.getStyleClass().remove("active-menu");
        buttonDelivery.getStyleClass().remove("active-menu");
        buttonRecycleItems.getStyleClass().remove("active-menu");

        if (!active.getStyleClass().contains("active-menu")) {
            active.getStyleClass().add("active-menu");
        }
    }
}








    /*
    @FXML
    private void btnCustomer(MouseEvent event) {
        safeLoad("customer.fxml", buttonCustomer);
        }

    @FXML
    private void btnOrder(MouseEvent event) {
        safeLoad("order.fxml", buttonOrder);
    }
    @FXML
    private void btnPayment(MouseEvent event) {
        safeLoad("payement.fxml", buttonPayment);
    }
    @FXML
    private void btnDelivery(MouseEvent event) {
        safeLoad("delivery.fxml", buttonDelivery);
    }
    @FXML
    private void btnRecycleItems(MouseEvent event) {
        safeLoad("recycleItems.fxml", buttonRecycleItems);
    }
    @FXML
    private void btnProduct(MouseEvent event) {
        safeLoad("product.fxml", buttonProduct);
    }


}
/*
    @FXML
    private void handleButtonPaymentAction(MouseEvent event) {
        Object obj = event.getSource();
        if (obj == buttonPayment) {
            safeLoad("payement.fxml", buttonPayment);
        }
    }

    @FXML
    private void handleButtonDelivery(MouseEvent event) {
        Object obj = event.getSource();
        if (obj == buttonDelivery) {
            safeLoad("delivery.fxml", buttonDelivery);
        }
    }
    @FXML
    private void handleButtonRecycleItems(MouseEvent event) {
        Object obj = event.getSource();
        if (obj == buttonRecycleItems) {
            safeLoad("recycleItems.fxml", buttonRecycleItems);
        }
    }


    @FXML
    private void btnonCustomer()throws IOException {
        Parent Customer = App.loadFXML("customer");
        root.getChildren().setAll(Customer);
        System.out.println("Customer page loaded");

    }
        @FXML
        private void btnOrder()throws IOException{
            Parent Order = App.loadFXML("order");
            root.getChildren().setAll(Order);
            System.out.println("Order page loaded");
    }
    @FXML
    private void btnProduct()throws IOException {
        Parent Product = App.loadFXML("product");
        root.getChildren().setAll(Product);
        System.out.println("Product page loaded");
    }

    @FXML
    private void btnPayment()throws IOException {
        Parent Payment = App.loadFXML("payment");
        root.getChildren().setAll(Payment);
        System.out.println("Payment page loaded");
    }

    @FXML
    private void btnDelivery()throws IOException {
        Parent Delivery = App.loadFXML("delivery");
        root.getChildren().setAll(Delivery);
        System.out.println("Delivery page loaded");
    }

    @FXML
    private void btnRecycle()throws IOException {
        Parent Recycle= App.loadFXML("recycle");
        root.getChildren().setAll(Recycle);
        System.out.println("Recycle page loaded");
    }
*/







        // adminContentPane//
   /*  @FXML
    private void openCustomer() throws IOException {

         Parent Customer = App.loadFXML("Customer");
         root.getChildren().setAll(Customer);
         System.out.println("Customer page loaded");
    }

     @FXML
    private void openProduct(){
        safeLoad("product.fxml", buttonProduct);
    }

    @FXML
    private void openOrder(){
        safeLoad("order.fxml", buttonOrder);
    }

    @FXML
    private void openPayment(){
        safeLoad("payement.fxml", buttonPayment);
    }

    @FXML
    private void openDelivery(){
        safeLoad("delivery.fxml", buttonDelivery);
    }

    @FXML
    private void openRecycleItems(){
        safeLoad("recycleItems.fxml", buttonRecycleItems);
    }*/



/*
package lk.ijse.recycle;

import javafx.application.Application;
import javafx.fxml.FXMLLoader;
import javafx.scene.Scene;
import javafx.stage.Stage;

public class App extends Application {

    private static Scene scene;

    @Override
    public void start(Stage stage) throws Exception {
        scene = new Scene(loadFXML("login"));
        stage.setScene(scene);
        stage.setTitle("Recycle Management System");
        stage.show();
    }

    public static void setRoot(String fxml) throws Exception {
        scene.setRoot(loadFXML(fxml));
    }

    private static javafx.scene.Parent loadFXML(String fxml) throws Exception {
        FXMLLoader fxmlLoader =
                new FXMLLoader(App.class.getResource("/view/" + fxml + ".fxml"));
        return fxmlLoader.load();
    }

    public static void main(String[] args) {
        launch();
    }
}


public class DashboardController {

    @FXML
    private AnchorPane root;

    @FXML
    private Button buttonCustomer;
    @FXML
    private Button buttonOrder;
    @FXML
    private Button buttonPayment;
    @FXML
    private Button buttonDelivery;
    @FXML
    private Button buttonRecycleItems;

    private void safeLoad(String fxml) {
        try {
            Parent rootNode = FXMLLoader.load(
                    getClass().getResource("/view/" + fxml)
            );
            root.getChildren().setAll(rootNode);
        } catch (IOException e) {
            e.printStackTrace();
        }
    }

    @FXML
    void btnCustomer(ActionEvent event) {
        safeLoad("customer.fxml");
    }

    @FXML
    void btnOrder(ActionEvent event) {
        safeLoad("order.fxml");
    }

    @FXML
    void btnPayment(ActionEvent event) {
        safeLoad("payement.fxml");
    }

    @FXML
    void btnDelivery(ActionEvent event) {
        safeLoad("delivery.fxml");
    }

    @FXML
    void btnRecycleItems(ActionEvent event) {
        safeLoad("recycleItems.fxml");
    }
}

*/